local ScreenGui = Instance.new("ScreenGui")
local Frame = Instance.new("Frame")
local UICorner = Instance.new("UICorner")
local Title = Instance.new("TextLabel")
local Underline = Instance.new("Frame")
local Instructions = Instance.new("TextLabel")
local TextBox = Instance.new("TextBox")
local TextBoxCorner = Instance.new("UICorner")
local DiscordText = Instance.new("TextLabel")
local BottomLine = Instance.new("Frame")
local LoginButton = Instance.new("TextButton")
local LoginButtonCorner = Instance.new("UICorner")
local DiscordButton = Instance.new("TextButton")
local DiscordButtonCorner = Instance.new("UICorner")
local CloseButton = Instance.new("TextButton")
local NotificationFrame = Instance.new("Frame")
local NotificationText = Instance.new("TextLabel")
local NotificationCorner = Instance.new("UICorner")
local Popup = Instance.new("Frame")
local PopupCorner = Instance.new("UICorner")
local PopupText = Instance.new("TextLabel")
local ClosePopupButton = Instance.new("TextButton")
local ClosePopupButtonCorner = Instance.new("UICorner")

local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local SoundService = game:GetService("SoundService")

-- Ses efekti
local clickSound = Instance.new("Sound")
clickSound.SoundId = "rbxassetid://18662825104" -- Ses efektinin asset ID'si
clickSound.Parent = SoundService

-- GUI'nin özelliklerini ayarla
ScreenGui.Parent = game.CoreGui
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

Frame.Parent = ScreenGui
Frame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
Frame.Position = UDim2.new(0.5, 0, 0.5, 0)
Frame.AnchorPoint = Vector2.new(0.5, 0.5)
Frame.Size = UDim2.new(0, 650, 0, 250)
Frame.BorderSizePixel = 0

UICorner.Parent = Frame
UICorner.CornerRadius = UDim.new(0, 10)

Title.Parent = Frame
Title.Size = UDim2.new(1, -40, 0, 30)
Title.Position = UDim2.new(0, 20, 0, 10)
Title.Text = "TRESKOR HUB SLS HİTBOX"
Title.TextSize = 18
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.BackgroundTransparency = 1
Title.TextXAlignment = Enum.TextXAlignment.Left

Underline.Parent = Frame
Underline.Size = UDim2.new(1, -40, 0, 1)
Underline.Position = UDim2.new(0, 20, 0, 40)
Underline.BackgroundColor3 = Color3.fromRGB(0, 191, 255) -- Açık mavi

Instructions.Parent = Frame
Instructions.Size = UDim2.new(1, -40, 0, 20)
Instructions.Position = UDim2.new(0, 20, 0, 50)
Instructions.Text = "Join our Discord server to obtain the key."
Instructions.TextSize = 14
Instructions.TextColor3 = Color3.fromRGB(200, 200, 200)
Instructions.BackgroundTransparency = 1
Instructions.TextXAlignment = Enum.TextXAlignment.Left

TextBox.Parent = Frame
TextBox.Size = UDim2.new(1, -40, 0, 40)
TextBox.Position = UDim2.new(0, 20, 0, 75)
TextBox.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
TextBox.PlaceholderText = "Enter Key"
TextBox.Text = ""
TextBox.TextSize = 18
TextBox.TextColor3 = Color3.fromRGB(255, 255, 255)
TextBox.BorderColor3 = Color3.fromRGB(0, 191, 255) -- Açık mavi
TextBox.TextXAlignment = Enum.TextXAlignment.Left

TextBoxCorner.Parent = TextBox
TextBoxCorner.CornerRadius = UDim.new(0, 5)

BottomLine.Parent = Frame
BottomLine.Size = UDim2.new(1, -40, 0, 1)
BottomLine.Position = UDim2.new(0, 20, 0, 140)
BottomLine.BackgroundColor3 = Color3.fromRGB(0, 191, 255) -- Açık mavi

DiscordText.Parent = Frame
DiscordText.Size = UDim2.new(1, -40, 0, 20)
DiscordText.Position = UDim2.new(0, 20, 0, 150)
DiscordText.Text = "The key is updated every 24 hours."
DiscordText.TextSize = 14
DiscordText.TextColor3 = Color3.fromRGB(200, 200, 200)
DiscordText.BackgroundTransparency = 1
DiscordText.TextXAlignment = Enum.TextXAlignment.Left

LoginButton.Parent = Frame
LoginButton.Size = UDim2.new(0, 100, 0, 40)
LoginButton.Position = UDim2.new(1, -220, 0, 180)
LoginButton.BackgroundColor3 = Color3.fromRGB(0, 0, 139) -- Koyu mavi
LoginButton.Text = "Login"
LoginButton.TextSize = 18
LoginButton.TextColor3 = Color3.fromRGB(255, 255, 255)

LoginButtonCorner.Parent = LoginButton
LoginButtonCorner.CornerRadius = UDim.new(0, 5)

DiscordButton.Parent = Frame
DiscordButton.Size = UDim2.new(0, 100, 0, 40)
DiscordButton.Position = UDim2.new(1, -110, 0, 180)
DiscordButton.BackgroundColor3 = Color3.fromRGB(0, 153, 204) -- Az koyu mavi
DiscordButton.Text = "Get Key"
DiscordButton.TextSize = 18
DiscordButton.TextColor3 = Color3.fromRGB(255, 255, 255)

DiscordButtonCorner.Parent = DiscordButton
DiscordButtonCorner.CornerRadius = UDim.new(0, 5)

CloseButton.Parent = Frame
CloseButton.Size = UDim2.new(0, 30, 0, 30)
CloseButton.Position = UDim2.new(1, -35, 0, 5)
CloseButton.BackgroundTransparency = 1
CloseButton.Text = "×"
CloseButton.TextScaled = true
CloseButton.TextColor3 = Color3.fromRGB(150, 150, 150)

NotificationFrame.Parent = ScreenGui
NotificationFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
NotificationFrame.Size = UDim2.new(0, 300, 0, 50)
NotificationFrame.Position = UDim2.new(1, 1, 1, 60) -- Initially off-screen
NotificationFrame.AnchorPoint = Vector2.new(1, 1)
NotificationFrame.Visible = false
NotificationFrame.ClipsDescendants = true

NotificationText.Parent = NotificationFrame
NotificationText.Size = UDim2.new(1, -20, 1, -20)
NotificationText.Position = UDim2.new(0, 10, 0, 10)
NotificationText.Text = ""
NotificationText.TextSize = 18
NotificationText.TextColor3 = Color3.fromRGB(255, 255, 255)
NotificationText.BackgroundTransparency = 1

NotificationCorner.Parent = NotificationFrame
NotificationCorner.CornerRadius = UDim.new(0, 10)

Popup.Parent = ScreenGui
Popup.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
Popup.Position = UDim2.new(0.5, 0, 0.5, 0)
Popup.AnchorPoint = Vector2.new(0.5, 0.5)
Popup.Size = UDim2.new(0, 400, 0, 200)
Popup.Visible = false

PopupCorner.Parent = Popup
PopupCorner.CornerRadius = UDim.new(0, 10)

PopupText.Parent = Popup
PopupText.Size = UDim2.new(1, -20, 1, -60)
PopupText.Position = UDim2.new(0, 10, 0, 10)
PopupText.Text = "Key Not Correct!"
PopupText.TextSize = 18
PopupText.TextColor3 = Color3.fromRGB(255, 255, 255)
PopupText.BackgroundTransparency = 1

ClosePopupButton.Parent = Popup
ClosePopupButton.Size = UDim2.new(0, 100, 0, 40)
ClosePopupButton.Position = UDim2.new(0.5, -50, 1, -50)
ClosePopupButton.BackgroundColor3 = Color3.fromRGB(0, 0, 139)
ClosePopupButton.Text = "Close"
ClosePopupButton.TextSize = 18
ClosePopupButton.TextColor3 = Color3.fromRGB(255, 255, 255)

ClosePopupButtonCorner.Parent = ClosePopupButton
ClosePopupButtonCorner.CornerRadius = UDim.new(0, 5)

-- Hover Efekti
local function createHoverEffect(button)
    local originalColor = button.BackgroundColor3
    local hoverColor = Color3.fromRGB(0, 102, 204)
    local hoverTweenInfo = TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)

    button.MouseEnter:Connect(function()
        local tween = TweenService:Create(button, hoverTweenInfo, {BackgroundColor3 = hoverColor})
        tween:Play()
    end)
    button.MouseLeave:Connect(function()
        local tween = TweenService:Create(button, hoverTweenInfo, {BackgroundColor3 = originalColor})
        tween:Play()
    end)
end

createHoverEffect(LoginButton)
createHoverEffect(DiscordButton)

-- Tooltip Efekti
local function createTooltip(button, text)
    local tooltip = Instance.new("TextLabel")
    tooltip.Parent = button
    tooltip.Text = text
    tooltip.TextSize = 14
    tooltip.TextColor3 = Color3.fromRGB(255, 255, 255)
    tooltip.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    tooltip.BackgroundTransparency = 0.7
    tooltip.Size = UDim2.new(0, 100, 0, 30)
    tooltip.Position = UDim2.new(0.5, -50, 0, -35)
    tooltip.AnchorPoint = Vector2.new(0.5, 0)
    tooltip.Visible = false

    local tooltipCorner = Instance.new("UICorner")
    tooltipCorner.Parent = tooltip
    tooltipCorner.CornerRadius = UDim.new(0, 5)

    local function showTooltip()
        tooltip.Visible = true
        local tween = TweenService:Create(tooltip, TweenInfo.new(0.2), {BackgroundTransparency = 0.5})
        tween:Play()
    end

    local function hideTooltip()
        local tween = TweenService:Create(tooltip, TweenInfo.new(0.2), {BackgroundTransparency = 0.7})
        tween.Completed:Connect(function()
            tooltip.Visible = false
        end)
        tween:Play()
    end

    button.MouseEnter:Connect(showTooltip)
    button.MouseLeave:Connect(hideTooltip)
end


createTooltip(DiscordButton, "dsc.gg/treskor")

-- Bildirim Gösterme Fonksiyonu
local function showNotification(text)
    NotificationText.Text = text
    NotificationFrame.Visible = true

    local showTweenInfo = TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.Out)
    local showTween = TweenService:Create(NotificationFrame, showTweenInfo, {Position = UDim2.new(1, -10, 1, -10), Size = UDim2.new(0, 300, 0, 60), BackgroundTransparency = 0})
    showTween:Play()

    showTween.Completed:Connect(function()
        wait(3)
        local hideTweenInfo = TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.In)
        local hideTween = TweenService:Create(NotificationFrame, hideTweenInfo, {Position = UDim2.new(1, 1, 1, 60), Size = UDim2.new(0, 300, 0, 50), BackgroundTransparency = 0.5})
        hideTween:Play()

        hideTween.Completed:Connect(function()
            NotificationFrame.Visible = false
        end)
    end)
end

-- Pop-up Gösterme Fonksiyonu
local function showPopup(text)
    PopupText.Text = text
    Popup.Visible = true
    Popup.Size = UDim2.new(0, 0, 0, 0) -- Başlangıç boyutunu sıfır yap
    Popup.BackgroundTransparency = 1 -- Başlangıçta arka plan transparan

    -- Pop-up açılış animasyonu
    local showTween = TweenService:Create(Popup, TweenInfo.new(0.5, Enum.EasingStyle.Bounce, Enum.EasingDirection.Out), {Size = UDim2.new(0, 400, 0, 200), BackgroundTransparency = 0})
    showTween:Play()
end

-- Pop-up Kapatma Fonksiyonu
ClosePopupButton.MouseButton1Click:Connect(function()
    local hideTween = TweenService:Create(Popup, TweenInfo.new(0.5, Enum.EasingStyle.Bounce, Enum.EasingDirection.In), {Size = UDim2.new(0, 0, 0, 0), BackgroundTransparency = 1})
    hideTween:Play()
    
    hideTween.Completed:Connect(function()
        Popup.Visible = false -- Pop-up görünürlüğünü kapat
    end)
end)
-- Login Button Click Event
LoginButton.MouseButton1Click:Connect(function()
    clickSound:Play()
    if TextBox.Text == "merhaba" then -- Doğru anahtar kontrolü burada yapılıyor.
        showNotification("Key Correct!"); -- Anahtar doğruysa bildirim göster.
        
        -- GUI'yi 3 saniye sonra kaybolma animasyonu ile kapat
        wait(3) -- 3 saniye bekle
        local fadeOutTween = TweenService:Create(Frame, TweenInfo.new(0.5, Enum.EasingStyle.Sine, Enum.EasingDirection.In), {BackgroundTransparency = 1}) -- Arka plan transparan
        fadeOutTween:Play()

        fadeOutTween.Completed:Connect(function()
            ScreenGui:Destroy() -- GUI'yi yok et

local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()

local Window = Fluent:CreateWindow({
    Title = "TRESKOR HUB " .. Fluent.Version,
    SubTitle = "SLS HİTBOX",
    TabWidth = 160,
    Size = UDim2.fromOffset(400, 189),
    Acrylic = true,
    Theme = "Dark",
    MinimizeKey = Enum.KeyCode.LeftControl
})

local Tabs = {
    Hitbox = Window:AddTab({ Title = "Hitbox", Icon = "eye" }),
    Credits = Window:AddTab({ Title = "Credits", Icon = "crown" })
}

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

local transparencyValue = 1 -- Tamamen görünmez

local function sendNotification(title, text)
    game.StarterGui:SetCore("SendNotification", {
        Title = title,
        Text = text,
        Duration = 3
    })
end

local function createAndStabilizePart(part, size, name, transparency)
    local existingClone = part.Parent:FindFirstChild(name)
    if existingClone then
        existingClone:Destroy()
    end

    local clone = Instance.new("Part")
    clone.Size = size
    clone.CanCollide = false
    clone.Transparency = transparency
    clone.Name = name
    clone.Position = part.Position
    clone.Anchored = false
    clone.Parent = part.Parent

    local originalAttachment = Instance.new("Attachment")
    originalAttachment.Name = "OriginalAttachment"
    originalAttachment.Parent = part

    local cloneAttachment = Instance.new("Attachment")
    cloneAttachment.Name = "CloneAttachment"
    cloneAttachment.Parent = clone

    local alignPosition = Instance.new("AlignPosition")
    alignPosition.Attachment0 = cloneAttachment
    alignPosition.Attachment1 = originalAttachment
    alignPosition.RigidityEnabled = true
    alignPosition.Parent = clone

    local alignOrientation = Instance.new("AlignOrientation")
    alignOrientation.Attachment0 = cloneAttachment
    alignOrientation.Attachment1 = originalAttachment
    alignOrientation.RigidityEnabled = true
    alignOrientation.Parent = clone

    return clone
end

local function expandParts()
    local character = LocalPlayer.Character
    if character then
        local partsToExpand = {
            "LowerTorso", "UpperTorso", "HumanoidRootPart",
            "Left Arm", "Right Arm", "Left Leg", "Right Leg",
            "LeftHand", "RightHand", "LeftFoot", "RightFoot"
        }

        for _, partName in pairs(partsToExpand) do
            local part = character:FindFirstChild(partName)
            if part then
                createAndStabilizePart(part, Vector3.new(12, 12, 12), partName .. "_Expanded", transparencyValue)
            end
        end
        
        sendNotification("Hitbox Enabled", "")
    end
end

local function removeParts()
    local character = LocalPlayer.Character
    if character then
        for _, part in pairs(character:GetChildren()) do
            if part:IsA("Part") and part.Name:match("_Expanded") then
                part:Destroy()
            end
        end
        sendNotification("Hitbox Disabled", "")
    end
end

local Toggle = Tabs.Hitbox:AddToggle("MyToggle", {Title = "Sls Hitbox", Default = false })

Toggle:OnChanged(function()
    if Toggle.Value then
        expandParts()
    else
        removeParts()
    end
end)

Tabs.Credits:AddParagraph({
        Title = "Akashi And Ensar",
        Content = "Discrod: l2vy / ensar.xr"
    })

local ScreenGui = Instance.new("ScreenGui")
local Frame = Instance.new("Frame")
local TextButton = Instance.new("TextButton")
local UITextSizeConstraint = Instance.new("UITextSizeConstraint")
local ImageLabel = Instance.new("ImageLabel")

-- Properties:

ScreenGui.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

Frame.Parent = ScreenGui
Frame.BackgroundColor3 = Color3.fromRGB(26, 26, 26)
Frame.BackgroundTransparency = 0.500
Frame.Position = UDim2.new(0.858712733, 0, 0.015, 0)  -- Yukarı taşındı
Frame.Size = UDim2.new(0.06, 0, 0.06, 0)  -- Kare boyutu
Frame.AnchorPoint = Vector2.new(0.5, 0.5)

-- Köşeleri yuvarlatma (UICorner ile)
local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(0, 12)  -- Köşeleri yuvarlatmak için 12px yuvarlama
UICorner.Parent = Frame

TextButton.Parent = Frame
TextButton.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
TextButton.BackgroundTransparency = 1.000
TextButton.Size = UDim2.new(1, 0, 1, 0)
TextButton.Font = Enum.Font.SourceSans
TextButton.Text = ""
TextButton.TextColor3 = Color3.fromRGB(0, 0, 0)
TextButton.TextScaled = true
TextButton.TextSize = 30.000
TextButton.TextStrokeColor3 = Color3.fromRGB(255, 255, 255)
TextButton.TextStrokeTransparency = 0.000
TextButton.TextWrapped = true
TextButton.MouseButton1Down:Connect(function()
	game:GetService("VirtualInputManager"):SendKeyEvent(true, "LeftControl", false , game)
end)

UITextSizeConstraint.Parent = TextButton
UITextSizeConstraint.MaxTextSize = 30

-- İkon ekleme (Roblox ikonu)
ImageLabel.Parent = Frame
ImageLabel.BackgroundTransparency = 1
ImageLabel.Size = UDim2.new(1, 0, 1, 0)  -- İkon tüm GUI'yi kaplayacak şekilde
ImageLabel.Position = UDim2.new(0, 0, 0, 0)
ImageLabel.Image = "rbxassetid://109972092483819"  -- Roblox ikonu

-- Resim için köşe yuvarlama (ImageLabel'a UICorner ekle)
local UICornerForImage = Instance.new("UICorner")
UICornerForImage.CornerRadius = UDim.new(0, 12)  -- Resmin köşelerini yuvarlamak için 12px yuvarlama
UICornerForImage.Parent = ImageLabel

-- Sürüklenebilir fonksiyonalite
local userInputService = game:GetService("UserInputService")
local dragging = false
local dragInput, mousePos, framePos

TextButton.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 then
		dragging = true
		dragInput = input
		mousePos = userInputService:GetMouseLocation()
		framePos = Frame.Position
		-- Karakterin durmasını engellemek ve mouse ikonunu gizlemek
		userInputService.MouseIconEnabled = false
		game:GetService("Players").LocalPlayer.Character.Humanoid.WalkSpeed = 16  -- Karakterin durmasını engelle
	end
end)

TextButton.InputEnded:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 then
		dragging = false
		-- Mouse ikonunu geri aç
		userInputService.MouseIconEnabled = true
	end
end)

game:GetService("RunService").RenderStepped:Connect(function()
	if dragging then
		local delta = userInputService:GetMouseLocation() - mousePos
		Frame.Position = UDim2.new(framePos.X.Scale, framePos.X.Offset + delta.X, framePos.Y.Scale, framePos.Y.Offset + delta.Y)
	end
end)
        end)
    else
        showPopup("Key Not Correct!"); -- Anahtar yanlışsa pop-up göster.
    end  
end)

-- Discord Button Click Event
DiscordButton.MouseButton1Click:Connect(function()
    clickSound:Play()
    setclipboard("https://dsc.gg/treskorhub")
    showNotification("Discord link copied!")
end)

-- Close Button Click Event
CloseButton.MouseButton1Click:Connect(function()
    ScreenGui:Destroy()
end)

-- GUI'yi sürüklenebilir yap
local dragging
local dragInput
local dragStart
local startPos

local function update(input)
    local delta = input.Position - dragStart
    Frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
end

Frame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = Frame.Position

        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

Frame.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        dragInput = input
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if input == dragInput and dragging then
        update(input)
    end
end)

local function animateButtonClick(button)
    local originalSize = button.Size
    local shrinkSize = UDim2.new(originalSize.X.Scale, originalSize.X.Offset - 10, originalSize.Y.Scale, originalSize.Y.Offset - 10)
    local tweenInfo = TweenInfo.new(0.1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)

    button.MouseButton1Click:Connect(function()
        local shrinkTween = TweenService:Create(button, tweenInfo, {Size = shrinkSize})
        local growTween = TweenService:Create(button, tweenInfo, {Size = originalSize})
        shrinkTween:Play()
        shrinkTween.Completed:Connect(function()
            growTween:Play()
        end)
    end)
end

animateButtonClick(LoginButton)
animateButtonClick(DiscordButton)

-- GUI'yi açma animasyonu
local function openGui()
    Frame.Position = UDim2.new(0.5, 0, 0.5, 0) -- Merkeze yerleştir
    Frame.Size = UDim2.new(0, 0, 0, 0) -- Başlangıç boyutunu sıfır yap
    Frame.BackgroundTransparency = 1 -- Başlangıçta transparan
    Frame.Visible = true

    -- Yavaşça genişleyip görünür hale getirme animasyonu
    local tweenInfo = TweenInfo.new(1, Enum.EasingStyle.Bounce, Enum.EasingDirection.Out)
    local tweenGoal = { Position = UDim2.new(0.5, -325, 0.5, -125), Size = UDim2.new(0, 650, 0, 250), BackgroundTransparency = 0 }
    local tween = TweenService:Create(Frame, tweenInfo, tweenGoal)
    tween:Play()
end

-- GUI'yi açmak için çağırın
openGui()